---
interface Props {
  class?: string;
  title: string;
  videoid: string;
}

const { title, videoid, class: className } = Astro.props;
---

<lite-youtube 
  videoid={videoid}
  videotitle={title}
  class={`${className} w-[400px] h-[250px] rounded-lg shadow-xl bg-black/75`}
></lite-youtube>
